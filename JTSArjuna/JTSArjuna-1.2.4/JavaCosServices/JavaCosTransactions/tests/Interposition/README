NOTE: this example will only be built when using ORBacus or VisiBroker
since we cannot support implicit transaction propagation/interposition
in other Orbs.

Server: TranGridServer

If using OrbixWeb then the registered server name should be TranGrid.
